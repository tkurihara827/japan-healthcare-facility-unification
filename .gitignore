# 1. ユーザーが手動で配置/プログラムが生成するデータディレクトリ
#    これらのディレクトリは、コードとは別にローカルで管理されます。
r0512/          # 中間処理データ PROC_DIR
r0512merge/     # 最終/連携データ MERGE_DIR
r0512raw/       # 生データ格納用 RAW_DIR (ユーザーが手動でデータを入れる)

# 2. プログラム実行中に生成される主要なデータファイル
#    (MERGE_DIR内に生成されるファイル)
total.xlsx      # append.pyの中間出力
total2.xlsx     # gis.pyの入力 / append.pyの最終出力（診療科フラグまで）
total3.xlsx     # gis.pyの最終出力（緯度経度含む）
address.csv     # ジオコーディング連携用の入力ファイル
address_out.csv # ジオコーディング連携後の結果ファイル (ユーザーが手動で配置)

# 3. Python固有のファイル
__pycache__/    # Pythonが生成するキャッシュディレクトリ
*.pyc           # コンパイルされたバイトコードファイル
.ipynb_checkpoints # Jupyter Notebookのチェックポイント

# 4. OS/エディタ固有の一時ファイル
.DS_Store       # macOS固有のファイル
*~              # Emacsなど一部エディタの一時ファイル
